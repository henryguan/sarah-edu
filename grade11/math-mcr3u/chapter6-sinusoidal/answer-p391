
# Page - 391
![See diagram](images/p391-1.png)

## Answer 1:

We will use the general equation:
$$y = a \cos(b(x - c)) + d \quad \text{or} \quad y = a \sin(b(x - c)) + d$$

Where the $b$-value is calculated using:
$$b = \frac{360^\circ}{\text{Period}}$$

Let's re-analyze each function using the $360^\circ$ context.

---

## 1. Function (i) - Blue Curve

### A. Vertical Analysis ($a$ and $d$)
* **Maximum ($y_{\max}$):** 8
* **Minimum ($y_{\min}$):** 2
* **Vertical Shift ($d$):** $d = \frac{8 + 2}{2} = \mathbf{5}$
* **Amplitude ($a$):** $a = 8 - 5 = \mathbf{3}$

### B. Horizontal Analysis ($P$ and $b$)
* **Period ($P$):** The distance between maxima is $8 - 4 = \mathbf{4}$ units.
* **$b$-value:**
    $$b = \frac{360^\circ}{P} = \frac{360^\circ}{4} = \mathbf{90^\circ}$$

### C. Phase Shift ($c$) and Equation
We'll use **cosine** since the graph starts a cycle at a maximum.
* The first maximum occurs at $x = 4$.
* **Phase Shift ($c$):** $\mathbf{c = 4}$

**Equation for (i):**
$$y = 3 \cos(90^\circ(x - 4)) + 5$$

---

## 2. Function (ii) - Black Curve

### A. Vertical Analysis ($a$ and $d$)
* **Maximum ($y_{\max}$):** 1
* **Minimum ($y_{\min}$):** 0
* **Vertical Shift ($d$):** $d = \frac{1 + 0}{2} = \mathbf{0.5}$
* **Amplitude ($a$):** $a = 1 - 0.5 = \mathbf{0.5}$

### B. Horizontal Analysis ($P$ and $b$)
* **Period ($P$):** The distance between maxima (e.g., $x=1$ to $x=5$) is $5 - 1 = \mathbf{4}$ units.
* **$b$-value:**
    $$b = \frac{360^\circ}{4} = \mathbf{90^\circ}$$

### C. Phase Shift ($c$) and Equation
We'll use **cosine** since the first cycle starts at a maximum.
* The first maximum occurs at $x = 1$.
* **Phase Shift ($c$):** $\mathbf{c = 1}$

**Equation for (ii):**
$$y = 0.5 \cos(90^\circ(x - 1)) + 0.5$$

---

## 3. Function (iii) - Pink Curve

### A. Vertical Analysis ($a$ and $d$)
* **Maximum ($y_{\max}$):** -1
* **Minimum ($y_{\min}$):** -3
* **Vertical Shift ($d$):** $d = \frac{-1 + (-3)}{2} = \mathbf{-2}$
* **Amplitude ($a$):** $a = -1 - (-2) = \mathbf{1}$

### B. Horizontal Analysis ($P$ and $b$)
* **Period ($P$):** The distance between two consecutive points where the graph crosses the midline going down (e.g., $x=0$ to $x=4$) is $4 - 0 = \mathbf{4}$ units.
* **$b$-value:**
    $$b = \frac{360^\circ}{4} = \mathbf{90^\circ}$$

### C. Phase Shift ($c$) and Equation
We'll use **sine**. Since the graph passes through the midline ($y=-2$) at $x=0$ and immediately goes **down** to the minimum, we use a **negative amplitude** ($a = -1$).
* **Phase Shift ($c$):** **0 units**. $\mathbf{c = 0}$

**Equation for (iii):**
$$y = -1 \sin(90^\circ x) - 2 \quad \text{or} \quad y = -\sin(90^\circ x) - 2$$


## Answer 2:

This is a great exercise in modeling a sinusoidal function from data points! The key requirement is to find an equation that **does not involve a horizontal translation** (phase shift, $c=0$). This means we must choose between a sine or cosine function based on where the cycle starts at $x=0^\circ$.

We will use the general equation:
$$y = a \sin(b x) + d \quad \text{or} \quad y = a \cos(b x) + d$$

### 1. Vertical Analysis (Finding $a$ and $d$)

We use the maximum and minimum values in the $y$ row to find the vertical shift ($d$) and the amplitude ($a$).

* **Maximum Value ($y_{\max}$):** 9
* **Minimum Value ($y_{\min}$):** 5

* **Vertical Shift ($d$ - Midline):**
    $$d = \frac{y_{\max} + y_{\min}}{2} = \frac{9 + 5}{2} = \frac{14}{2} = \mathbf{7}$$
    *(The midline is $y=7$.)*

* **Amplitude ($a$):**
    $$a = y_{\max} - d = 9 - 7 = \mathbf{2}$$

### 2. Horizontal Analysis (Finding $P$ and $b$)

The period ($P$) is the length of one complete cycle.

* **Period ($P$):** The data starts at a maximum ($y=9$) at $x=0^\circ$ and returns to a maximum ($y=9$) at $x=180^\circ$.
    $$P = 180^\circ - 0^\circ = \mathbf{180^\circ}$$

* **$b$-value (Horizontal Stretch/Compression):** We use the degree-based formula:
    $$b = \frac{360^\circ}{P} = \frac{360^\circ}{180^\circ} = \mathbf{2}$$

### 3. Choosing the Function (No Phase Shift)

We look at the value of the function when $x=0^\circ$:

* At $x=0^\circ$, $y=9$, which is the **maximum** value.

* A standard **cosine** function, $y = \cos(x)$, starts at its maximum value when $x=0$.
* A standard **sine** function, $y = \sin(x)$, starts at its midline value when $x=0$.

Since the function starts at its maximum at $x=0^\circ$, we must use the **cosine function** to ensure the phase shift ($c$) is zero.

### 4. Final Equation

Substitute the calculated values into the general cosine equation with $c=0$:
$$y = a \cos(b x) + d$$
$$y = 2 \cos(2 x) + 7$$

---

## The Function that Models the Data is:
$$y = 2 \cos(2x) + 7$$

## Answer 3

A sinusoidal function has an amplitude of 4 units, a period of $120^\circ$ degree and a maximum at (0, 9). Determine the equation of the function.


A:
We will use the standard form with the $360^\circ$ period:

$$y = a \cos(b(x - c)) + d$$

### 1. Determine Amplitude ($a$)

The amplitude is given directly:
* **Amplitude ($a$):** $\mathbf{4}$ units

### 2. Determine the $b$-value (Horizontal Stretch)

The $b$-value relates the standard period ($360^\circ$) to the given period ($P = 120^\circ$):
* **Period ($P$):** $120^\circ$
* **$b$-value:**
    $$b = \frac{360^\circ}{P} = \frac{360^\circ}{120^\circ} = \mathbf{3}$$

### 3. Determine the Vertical Shift ($d$)

The vertical shift ($d$) is the midline, which is the maximum height minus the amplitude:
* **Maximum Value ($y_{\max}$):** 9
* **Vertical Shift ($d$):**
    $$d = y_{\max} - a = 9 - 4 = \mathbf{5}$$

*(The midline is $y=5$. This also means the minimum value is $5 - 4 = 1$.)*

### 4. Determine the Phase Shift ($c$) and Function Type

We look at the location of the maximum:
* **Maximum Location:** $(0, 9)$

* A standard **cosine** function, $y = a \cos(x) + d$, naturally starts at its maximum when $x=0$.
* Since the maximum occurs exactly at $x=0$, we can choose the **cosine function** with **no phase shift**.
* **Phase Shift ($c$):** $\mathbf{0}$

### 5. Final Equation

Substitute the calculated values ($a=4$, $b=3$, $c=0$, $d=5$) into the cosine equation:

$$y = 4 \cos(3(x - 0)) + 5$$

$$\mathbf{y = 4 \cos(3x) + 5}$$


##4.a

This is a request identical to a previous one. I will re-extract the necessary information from the provided graph and determine the equation for each of the three sinusoidal functions, (i), (ii), and (iii).

The general equation for a sinusoidal function is:
$$y = a \sin(b(x - c)) + d \quad \text{or} \quad y = a \cos(b(x - c)) + d$$

Since the axes are unlabeled for degrees/radians, we will use the standard degree-based period where $b = \frac{360^\circ}{\text{Period}}$.

## 1. Function (i) - Blue Curve

### A. Vertical Analysis ($a$ and $d$)
* **Maximum Value ($y_{\max}$):** 8
* **Minimum Value ($y_{\min}$):** 2
* **Vertical Shift ($d$ - Midline):** $d = \frac{8 + 2}{2} = \mathbf{5}$
* **Amplitude ($a$):** $a = 8 - 5 = \mathbf{3}$

### B. Horizontal Analysis ($P$ and $b$)
* **Period ($P$):** The distance between maxima (from $x=4$ to $x=8$) is $8 - 4 = \mathbf{4}$ units.
* **$b$-value:** $b = \frac{360^\circ}{4} = \mathbf{90^\circ}$

### C. Phase Shift ($c$) and Equation
* **Function Type:** The graph starts a cycle at a maximum at $x=4$. We use the **cosine** function.
* **Phase Shift ($c$):** Shifted **4 units right**. $\mathbf{c = 4}$

$$\mathbf{y = 3 \cos(90^\circ(x - 4)) + 5}$$

---

## 2. Function (ii) - Black Curve

### A. Vertical Analysis ($a$ and $d$)
* **Maximum Value ($y_{\max}$):** 1.5 (or 1.6, we'll estimate 1.5 from the grid)
* **Minimum Value ($y_{\min}$):** 0.5 (estimated)
* **Vertical Shift ($d$ - Midline):** $d = \frac{1.5 + 0.5}{2} = \mathbf{1}$
* **Amplitude ($a$):** $a = 1.5 - 1 = \mathbf{0.5}$

### B. Horizontal Analysis ($P$ and $b$)
* **Period ($P$):** The distance between maxima (from $x=1$ to $x=5$) is $5 - 1 = \mathbf{4}$ units.
* **$b$-value:** $b = \frac{360^\circ}{4} = \mathbf{90^\circ}$

### C. Phase Shift ($c$) and Equation
* **Function Type:** The graph starts a cycle at a maximum at $x=1$. We use the **cosine** function.
* **Phase Shift ($c$):** Shifted **1 unit right**. $\mathbf{c = 1}$

$$\mathbf{y = 0.5 \cos(90^\circ(x - 1)) + 1}$$

---

## 3. Function (iii) - Pink Curve

### A. Vertical Analysis ($a$ and $d$)
* **Maximum Value ($y_{\max}$):** -1
* **Minimum Value ($y_{\min}$):** -3
* **Vertical Shift ($d$ - Midline):** $d = \frac{-1 + (-3)}{2} = \mathbf{-2}$
* **Amplitude ($a$):** $a = -1 - (-2) = \mathbf{1}$

### B. Horizontal Analysis ($P$ and $b$)
* **Period ($P$):** The distance between maxima (from $x=2$ to $x=6$) is $6 - 2 = \mathbf{4}$ units.
* **$b$-value:** $b = \frac{360^\circ}{4} = \mathbf{90^\circ}$

### C. Phase Shift ($c$) and Equation
* **Function Type:** The graph passes through the midline ($y=-2$) at $x=0$ and immediately goes **down** to the minimum. We use the **sine** function with a **negative amplitude** ($a = -1$).
* **Phase Shift ($c$):** **0 units**. $\mathbf{c = 0}$

$$\mathbf{y = -\sin(90^\circ x) - 2}$$

---

## Summary of Final Equations

| Function | Amplitude ($a$) | Midline ($d$) | $b$-value | Phase Shift ($c$) | Equation |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **(i)** | 3 | 5 | $90^\circ$ | 4 | $y = 3 \cos(90^\circ(x - 4)) + 5$ |
| **(ii)** | 0.5 | 1 | $90^\circ$ | 1 | $y = 0.5 \cos(90^\circ(x - 1)) + 1$ |
| **(iii)** | 1 | -2 | $90^\circ$ | 0 | $y = -\sin(90^\circ x) - 2$ |


---

## 5.a

The goal is to determine the equation of the function that is the **simplest model** (i.e., no horizontal translation, $c=0$). We will use the degree-based equation:
$$y = a \sin(b x) + d \quad \text{or} \quad y = a \cos(b x) + d$$

### Data Table Analysis

| $x$ | $0^\circ$ | $30^\circ$ | $60^\circ$ | $90^\circ$ | $120^\circ$ | $150^\circ$ | $180^\circ$ |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| $y$ | 3 | 2 | 1 | 2 | 3 | 2 | 1 |

### 1. Vertical Analysis (Amplitude $a$ and Vertical Shift $d$)

* **Maximum Value ($y_{\max}$):** 3
* **Minimum Value ($y_{\min}$):** 1

* **Vertical Shift ($d$ - Midline):**
    $$d = \frac{y_{\max} + y_{\min}}{2} = \frac{3 + 1}{2} = \frac{4}{2} = \mathbf{2}$$
* **Amplitude ($a$):**
    $$a = y_{\max} - d = 3 - 2 = \mathbf{1}$$

### 2. Horizontal Analysis (Period $P$ and $b$-value)

* **Period ($P$):** The data starts at a maximum ($y=3$) at $x=0^\circ$ and completes one full cycle back to a maximum ($y=3$) at $x=120^\circ$.
    $$P = 120^\circ - 0^\circ = \mathbf{120^\circ}$$
* **$b$-value:**
    $$b = \frac{360^\circ}{P} = \frac{360^\circ}{120^\circ} = \mathbf{3}$$

### 3. Choosing the Function (Simplest Model, $c=0$)

* At $x=0^\circ$, $y=3$, which is the **maximum** value.
* The standard **cosine** function starts at its maximum when $x=0$.

Therefore, we use the **cosine function** with no phase shift ($c=0$).

### 4. Final Equation

Substitute the calculated values ($a=1$, $b=3$, $d=2$) into the cosine equation $y = a \cos(b x) + d$:

$$\mathbf{y = \cos(3x) + 2}$$